# Changelog

Notable changes to the app, recorded as they happen. Newest entries at the top.

## Unreleased

- Project scaffolded from the default `flutter create` template.
- Documentation site set up with MkDocs Material (`docs/`, `mkdocs.yml`).
